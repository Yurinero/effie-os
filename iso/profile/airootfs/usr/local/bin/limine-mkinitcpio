#!/bin/sh
# Safe limine-mkinitcpio wrapper for live archiso environment
if [[ ! -d /sys/firmware/efi ]] || [[ ! -f /etc/default/limine ]]; then
    exit 0
fi

if [[ -x /usr/bin/limine-mkinitcpio ]]; then
    exec /usr/bin/limine-mkinitcpio "$@"
fi
exit 0
